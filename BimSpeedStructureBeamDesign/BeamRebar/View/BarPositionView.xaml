<Window x:Class="BimSpeedStructureBeamDesign.BeamRebar.View.BarPositionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:BimSpeedStructureBeamDesign.Properties"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource BEAMREBAR_BARPOSTITION_TITLE}"
        WindowStartupLocation="CenterScreen"
        Name="MainView"
        Height="200"
        Icon="/BimSpeedUtils;component/WPFUtils/bimspeed.ico"
        ResizeMode="NoResize"
        SizeToContent="Width">
    <FrameworkElement.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/BimSpeedStructureBeamDesign;component/Resources/ResourceString.en.xaml" />
                <ResourceDictionary Source="pack://application:,,,/BimSpeedUtils;component/WPFUtils/ResourceDictionaries/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Button" BasedOn="{StaticResource ResourceKey=ButtonStyle}" />
            <Style TargetType="CheckBox" BasedOn="{StaticResource ResourceKey=CheckBoxStyle}" />
            <Style TargetType="RadioButton" BasedOn="{StaticResource ResourceKey=RadioButtonStyle}" />
            <Style TargetType="GroupBox" BasedOn="{StaticResource ResourceKey=GroupBoxStyle}" />
            <Style TargetType="ComboBox" BasedOn="{StaticResource ResourceKey=ComboBoxStyle}" />
            <!--<Style TargetType="ToggleButton" BasedOn="{StaticResource ResourceKey=ToggleButtonStyle}" />-->
            <Style TargetType="TextBox" BasedOn="{StaticResource ResourceKey=TextBoxStyle}" />
            <Style TargetType="ProgressBar" BasedOn="{StaticResource ResourceKey=ProgressBarStyle}" />

        </ResourceDictionary>
    </FrameworkElement.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="55"></RowDefinition>
        </Grid.RowDefinitions>
        <GroupBox Header="{DynamicResource BEAMREBAR_BEAMREBARVIEW1_POSITION}">
            <ListBox VerticalAlignment="Center"
                     HorizontalAlignment="Center"
                     ItemsSource="{Binding RebarPointsInSection}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="80"
                                    Orientation="Vertical">
                            <TextBlock  HorizontalAlignment="Center"
                                        Text="{Binding Index}"></TextBlock>
                            <CheckBox IsChecked="{Binding Checked}"
                                      HorizontalAlignment="Center">
                            </CheckBox>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <Grid Height="55"
              Grid.Row="1">
            <StackPanel HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button IsCancel="True"
                        Width="80"
                        Height="25"
                        Name="Close"
                        Click="Close_OnClick"
                        Content="{DynamicResource BEAMREBAR_TITLE_CLOSE}"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>